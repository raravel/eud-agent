---
created: '2026-06-04'
depends_on: []
id: EUD-031-01ec
parent: EUD-026-4c16
priority: high
scope:
- panel/**
- .gitignore
- server/tests/test_panel_static.py
status: in_progress
title: 'Panel scaffold: Vite + React + TS + Tailwind + shadcn + AI Elements + Monaco
  wiring'
type: task
updated: '2026-06-04'
---

## Description
Scaffold the React panel toolchain in `panel/` (the Vite app root, per Decision 03): Vite + React 19 + TypeScript + Tailwind v4 (@tailwindcss/vite, CSS-variables mode) + shadcn/ui initialized for Vite + Vercel AI Elements components vendored shadcn-style (`npx ai-elements@latest` — component SOURCE lands in `panel/components/ai-elements/`; no runtime CDN/registry dependency) + Monaco (`monaco-editor` + `@monaco-editor/react`) wired to the npm bundle via `loader.config({ monaco })` and explicit Vite worker setup (default CDN loader forbidden — rules.md). `@/` path alias per shadcn convention. npm scripts: dev/build/preview. `.gitignore` += `panel/dist/`, `panel/node_modules/`.

SEQUENCING NOTE: `panel/index.html` becomes the Vite template (vanilla content replaced NOW); `panel/app.js`/`panel/style.css` stay as DEAD files until EUD-035 deletes them (selfcheck PANEL_FILES still requires all 3 until the switchover). Therefore `server/tests/test_panel_static.py` MUST be revised in this task to the React-source contract: package.json deps present (react, vite, tailwindcss, monaco-editor), vendored `panel/components/ai-elements/` + `panel/components/ui/` exist, `panel/dist/`+`node_modules/` gitignored, no http(s) URLs in the Vite template, no-BOM; dist-content checks SKIP with a note when dist absent. Drop the vanilla element-id/app.js checks (they move to runtime verification in EUD-034).

After the scaffold, re-ground tech-stack.md frontend pins against the real `panel/package.json` (report exact versions for the orchestrator's harness sync).

## Spec References
- [[features/03_agent-panel|03_agent-panel]] `../docs/features/03_agent-panel.md` — Toolchain / layout, Verification contract
- [[decisions/03_react-panel-rebuild|03_react-panel-rebuild]] `../docs/decisions/03_react-panel-rebuild.md`
- [[decisions/05_monaco-editor-adoption|05_monaco-editor-adoption]] `../docs/decisions/05_monaco-editor-adoption.md`
- [[rules]] `../docs/rules.md` — Server and panel (no runtime CDN; dist never committed)

## Completion Criteria
- [ ] `npm --prefix panel install` + `npm --prefix panel run build` exit 0; `panel/dist/index.html` produced
- [ ] Built `panel/dist/index.html` has zero external-origin script/link URLs (everything relative/bundled), incl. Monaco workers
- [ ] AI Elements + shadcn/ui components vendored as source under `panel/components/`; placeholder App renders a Conversation component and a Monaco editor instance (build-level proof both libraries wire up)
- [ ] `panel/dist/` and `panel/node_modules/` gitignored; `git status` clean after build
- [ ] Revised `server/tests/test_panel_static.py` passes (React-source contract; dist checks skip-aware); full pytest suite passes
- [ ] `panel/app.js` / `panel/style.css` untouched (dead until EUD-035); smoke stage (`--selfcheck`) still exits 0