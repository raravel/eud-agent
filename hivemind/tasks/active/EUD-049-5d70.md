---
created: '2026-06-05'
depends_on:
- EUD-048-d732
id: EUD-049-5d70
parent: EUD-044-c735
priority: high
scope:
- bridge/ZZZ_10_agent_bridge.lua
- server/eud_agent/bridge_io.py
- server/tests/test_bridge_datx_static.py
status: pending
title: Bridge DAT surface commands (resolver table + XDAT/TBL/RESET/REQ/BTN)
type: task
updated: '2026-06-05'
---

## Description
Bridge DAT surface per spec: replace the GETDAT/SETDAT resolver with a bridge-local name-to-enum table over SCDatFiles+DatFiles (units..orders + portdata + sfxdata, bypassing GetDatFileE), and add GETXDAT/SETXDAT (statusinfor/wireframe/ButtonSet via get_ExtraDatBinding), GETTBL/SETTBL (StatTxtBinding, value in BODY for UTF-8), RESETDAT (DataReset routing), GETREQ/SETREQ (RequireDataBinding + PasteCopyData string round-trip), GETBTN/SETBTN (CButtonSet GetCopyString/PasteFromString + manual SetDirty). bridge_io client wrappers for each.

## Spec References
- [[features/04_bridge-v2-surface|04_bridge-v2-surface]] `../docs/features/04_bridge-v2-surface.md` — DAT surface (B1)
- [[capability-survey]] `../docs/capability-survey.md` — rows 1-8

## Completion Criteria
- [ ] Static tests pin: resolver table covers portdata+sfxdata; each new dispatcher branch exists; model tokens get_ExtraDatBinding / StatTxtBinding / RequireDataBinding / PasteCopyData / PasteFromString present in the right branches; SETBTN branch calls SetDirty
- [ ] SETXDAT result returns the read-back .Value (Byte setters swallow bad values)
- [ ] SETTBL takes the value from the body (never the arg line); NULLSTRING resets
- [ ] bridge_io wrappers + arg validation helpers exist with unit tests
- [ ] ruff + full pytest green; live LUA-channel smoke on at least GETXDAT/GETTBL during the next editor session (user-assisted, may trail the merge)