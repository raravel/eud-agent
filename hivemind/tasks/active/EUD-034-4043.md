---
created: '2026-06-04'
depends_on:
- EUD-033-a00f
id: EUD-034-4043
parent: EUD-026-4c16
priority: high
scope:
- panel/src/**
- panel/components/**
status: pending
title: Panel UI feature parity (conversation, picker, review tabs, Monaco edit, apply)
type: task
updated: '2026-06-04'
---

## Description
Full UI feature parity with the verified vanilla panel, built on the EUD-033 client/state core and the vendored AI Elements/shadcn components: header (title, project name, connection state), Conversation/Message chat log (instructions, progress entries with spinner on the active stage incl. waiting_build, errors, applied confirmations), target picker (Select fed by `list`; GUI/settable=false options disabled with tooltip "읽기 전용 파일 형식"; refresh; no-project placeholder "프로젝트를 열어주세요" with instruct disabled), new-file toggle + NEWEPS filename input with inline validation errors, PromptInput instruction box with useContext checkbox (default ON) and Send gating, review area Tabs — preview (CodeBlock, escaped, lang label, 1 MiB display truncation with notice using consistent UTF-16 code-unit measure+slice), diff (server-supplied unified diff with per-line +/- coloring, hunk/file-header styling; hidden in new-file mode), edit (Monaco, seeded from the code event; Monaco buffer is the single source of truth for Apply) — dismissible advisory diagnostics strip that never blocks Apply, Apply SET / Apply NEWEPS / Cancel with state-correct enable/disable. Korean labels throughout. Dark editor-like theme via Tailwind.

Runtime verification is orchestrator-driven after this task: real browser against the mock WS server, walking the same checklist that validated the vanilla panel (connect/list/GUI-disabled/instruct/progress/code/diff/Monaco-edit-is-applied/NEWEPS validation+duplicate/reconnect mid-session).

## Spec References
- [[features/03_agent-panel|03_agent-panel]] `../docs/features/03_agent-panel.md` — UI layout, Behaviors, Edge cases
- [[decisions/05_monaco-editor-adoption|05_monaco-editor-adoption]] `../docs/decisions/05_monaco-editor-adoption.md`

## Completion Criteria
- [ ] `npm --prefix panel run build` exit 0; built dist has zero external-origin URLs
- [ ] vitest suite extended for component-level logic where headless-testable; passes
- [ ] Orchestrator runtime verification passes in a real browser against the mock WS server (full vanilla-parity checklist incl. Monaco edit content being what Apply sends, reconnect re-request, unknown-type tolerance)
- [ ] Korean labels present; diagnostics never gate Apply (verified in the runtime pass)
- [ ] Full pytest suite still passes
CARRY-FORWARD from EUD-031 review (2026-06-04): the placeholder pulls @streamdown/code|mermaid|math|cjk via ai-elements message.tsx -> dist is 28 MB with a 5.58 MB eager entry chunk. When selecting the real panel components, PRUNE the unused heavy transitive deps (@streamdown/mermaid|math|cjk) and the unused registry deps (@rive-app/react-webgl2, media-chrome, @xyflow/react, embla-carousel-react, cmdk, motion) - tree-shaken from dist today but the eager entry must shrink before shipping over the local origin. Also: vanilla-panel UX advisories to fix here are listed in the EUD-021 report (log cap, onError state, empty-project gating, truncation units).