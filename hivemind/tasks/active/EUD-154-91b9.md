---
created: '2026-06-12'
depends_on: []
id: EUD-154-91b9
parent: EUD-150-9296
priority: high
scope:
- src-tauri/src/engine.rs
status: pending
title: Add resident EPS_IDIOMS section to system prompt
type: task
updated: '2026-06-12'
---

## Description
Add a resident `EPS_IDIOMS` constant (target 800-1500 tokens, full budget) to `engine.rs` and
emit it in BOTH `build_system_prompt` and `resume_turn_text`, positioned AFTER `[first principles]`
and BEFORE `[reference context]`. This is the positive "write eps like this" anchor that stops the
model from falling back to SCMDraft classic triggers when retrieval misses. Covers the 10-12
most-miscoded constructs (entry functions, `$U`/`$L`, death-counter idiom, CreateUnit/Bring with
inverted locations, masked EPD read, setcurpl/getcurpl restore, bounded loop, production-token
button skill). Where an idiom overlaps an L0 prohibition, cross-reference the `first_principles`
item number instead of restating it.

## Spec References
- [[features/17_rag-knowledge-tiering|17_rag-knowledge-tiering]] `../docs/features/17_rag-knowledge-tiering.md` — L1 resident eps idioms
- [[rules]] `../docs/rules.md` — system prompt ordering (L0 > L1 > L2); first_principles always outranks
- [[architecture]] `../docs/architecture.md` — runtime flow / system prompt assembly

## Completion Criteria
- [ ] `EPS_IDIOMS` constant added with 10-12 positive, example-bearing eps idioms (800-1500 tok)
- [ ] Emitted in `build_system_prompt` AFTER [first principles], BEFORE [reference context]
- [ ] Emitted in `resume_turn_text` BEFORE [reference context]
- [ ] Ordering test asserts marker order: [first principles] < EPS_IDIOMS < [reference context]
- [ ] Overlapping items cross-reference L0 item numbers, do not restate prohibitions
- [ ] cargo test passes