---
created: '2026-06-04'
depends_on:
- EUD-034-4043
- EUD-030-75a4
id: EUD-035-2262
parent: EUD-026-4c16
priority: high
scope:
- panel/index.html
- panel/app.js
- panel/style.css
- server/eud_agent/config.py
- server/tests/test_config.py
- server/tests/test_panel_static.py
status: pending
title: 'Panel switchover: delete vanilla files, dist-serving selfcheck, revised contract
  test'
type: task
updated: '2026-06-04'
---

## Description
Final switchover from the vanilla panel to the React panel, after EUD-034's parity verification passed: delete the dead vanilla files (`panel/app.js`, `panel/style.css` — `panel/index.html` is already the Vite template since EUD-031), switch the server-side panel prerequisite to the BUILT output — `config.py` PANEL_FILES/selfcheck now requires `panel/dist/index.html` (+ built assets presence) instead of the three vanilla files, with the selfcheck failure message pointing at `npm --prefix panel run build` — and finalize `server/tests/test_panel_static.py` (drop any remaining vanilla-era checks; assert the dist-based selfcheck contract; dist checks stay skip-aware) and `server/tests/test_config.py` (panel fixture paths now dist-based). Update features/03 Implementation bindings via the orchestrator's harness sync as usual.

NOTE: this lands AFTER EUD-030 (config robustness) by dependency, so the config.py edits do not collide.

## Spec References
- [[features/03_agent-panel|03_agent-panel]] `../docs/features/03_agent-panel.md` — Verification contract
- [[features/02_python-server|02_python-server]] `../docs/features/02_python-server.md` — config.py (dist-based panel prerequisite), app.py (503 when dist missing)
- [[decisions/03_react-panel-rebuild|03_react-panel-rebuild]] `../docs/decisions/03_react-panel-rebuild.md`
- [[decisions/04_dist-release-distribution|04_dist-release-distribution]] `../docs/decisions/04_dist-release-distribution.md`

## Completion Criteria
- [ ] `panel/app.js` and `panel/style.css` deleted; no references remain anywhere in the repo (grep clean)
- [ ] `--selfcheck` exits 0 when `panel/dist/` is built, and fails with the build-hint message when dist is removed (both verified)
- [ ] test_panel_static.py + test_config.py updated; full pytest suite passes; ruff clean
- [ ] `npm --prefix panel run build` exit 0 (panel stage green)
CARRY-FORWARD from EUD-034 review (2026-06-04): (1) panel/components/ai-elements/conversation.tsx is vendored-but-dead (ConversationLog renders the chat); it alone keeps the 'ai' and 'use-stick-to-bottom' deps alive (tree-shaken from dist, zero runtime cost). Either delete it + drop both deps, or document retention. (2) tech-stack.md Frontend section needs the post-prune re-ground: 17 deps removed in EUD-034, vitest/happy-dom/@testing-library devDeps added. (3) cosmetic: header shows the project name with quotes — strip in orchestrator._parse_status (server-side, 1 line + test), not the panel.