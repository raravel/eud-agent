---
created: '2026-06-09'
depends_on:
- EUD-100-b403
id: EUD-132-0829
parent: EUD-091-4cb2
priority: medium
scope:
- src-tauri/src/lib.rs
- src-tauri/src/bootstrap.rs
status: pending
title: 'Wire bootstrap into app startup (lib.rs setup: ensure model/RAG, gate readiness,
  emit progress)'
type: task
updated: '2026-06-09'
---

## Description
Wire the first-run bootstrap into app startup. `bootstrap.rs` (EUD-100) is code-complete
(asset_status / verify_and_place / ensure_model / ensure_rag_index, sha256 + atomic place) but is
NEVER called — `lib.rs` only declares `pub mod bootstrap;`. The app currently starts without
ensuring the model/RAG-index assets exist.

- In the Tauri `setup` hook, resolve data dirs (config.rs `DataDirs`), check the manifest, and run
  the bootstrap flow (ensure RAG index from GitHub Release, ensure bge-m3 model) on a background
  task so the panel stays usable; emit `progress {stage: bootstrap}` (and the existing rag_warmup)
  to the panel. Missing/corrupt asset re-downloads; readiness is NOT gated on model load (lazy +
  background warmup per rules.md).
- Do not block the window; surface download progress/errors to the panel (consumed by EUD-120 UI).

## Spec References
- [[features/10_tauri-shell-bootstrap|10_tauri-shell-bootstrap]] `../docs/features/10_tauri-shell-bootstrap.md` — boot + first-run flow
- [[rules]] `../docs/rules.md` — RAG load must NOT gate readiness; sha256 + atomic place

## Completion Criteria
- [ ] App `setup` invokes the bootstrap flow (ensure RAG index + model) on a background task; not declared-only
- [ ] `progress {stage: bootstrap}` emitted to the panel; readiness not gated on model load (lazy warmup)
- [ ] Missing/corrupt asset re-downloads (sha256 + atomic place); failure surfaces to the panel, does not crash boot
- [ ] `cargo test -p eud-agent` passes; `cargo build -p eud-agent` links; clippy/fmt clean