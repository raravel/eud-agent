---
created: '2026-06-09'
depends_on:
- EUD-128-daea
id: EUD-131-6134
parent: EUD-127-e1a7
priority: medium
scope:
- src-tauri/src/tools.rs
status: pending
title: player_setup Rust tool handler (start locations + OWNR via playeredit)
type: task
updated: '2026-06-09'
---

## Description
Implement the `player_setup` WRITE tool handler in Rust (schema registered via EUD-124; handler
missing). Edits start-location units (type 214) + OWNR controllers on the connected map through
the playeredit path of the mapsafe rails + IsomEngine (EUD-128).

- Actions start/delstart/controller; validate args (player 1-8, tile coords, controller enum:
  human/computer/rescuable/neutral/inactive/closed) before any write.
- Encode playeredit ops (MapGenCli encoding) and apply via
  `MapSafe::write(map, OpKind::PlayerEdit, ops)`; the save keeps autoDefragmentLocations=false.
- Emit a journaled, reviewable changeset item; obeys mutation gate + budgets.

## Spec References
- [[features/08_map-info-tool|08_map-info-tool]] `../docs/features/08_map-info-tool.md` — OWNR/start-location semantics
- [[features/13_isom-ffi|13_isom-ffi]] `../docs/features/13_isom-ffi.md` — playeredit ops + mapsafe rails
- [[rules]] `../docs/rules.md` — map-write safety rails

## Completion Criteria
- [ ] `player_setup` handler implements start/delstart/controller with validate-before-write
- [ ] Writes go through `MapSafe::write(OpKind::PlayerEdit)` rails; autoDefragmentLocations=false preserved
- [ ] Produces a journaled, reviewable changeset item; mutation gate + budget honored
- [ ] `cargo test -p eud-agent` passes; clippy/fmt clean