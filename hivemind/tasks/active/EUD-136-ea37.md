---
created: '2026-06-10'
depends_on: []
id: EUD-136-ea37
parent: EUD-135-2f41
priority: medium
scope:
- ci/build_rag_index.rs
status: pending
title: build_rag_index reads in-repo corpus (--corpus, drop ECA default)
type: task
updated: '2026-06-10'
---

## Description
Modify the Rust index builder to read the corpus from an in-repo directory instead of the ECA
checkout. Replace the `--eca <dir>` flag (and its `DEFAULT_ECA_DIR` absolute path) with
`--corpus <dir>` defaulting to `ci/corpus`. The JSONL schema and the produced `rag-index.bin` /
`.sha256` / manifest are unchanged.

## Spec References
- [[features/16_rag-corpus-pipeline|16_rag-corpus-pipeline]] `../docs/features/16_rag-corpus-pipeline.md` — Embed section
- [[features/12_rust-rag-fastembed|12_rust-rag-fastembed]] `../docs/features/12_rust-rag-fastembed.md` — index format
- [[decisions/15_in-house-rag-corpus|15_in-house-rag-corpus]] `../docs/decisions/15_in-house-rag-corpus.md`

## Completion Criteria
- [ ] `--corpus <dir>` flag added; default is the repo-relative `ci/corpus`; the `--eca` flag and `DEFAULT_ECA_DIR` absolute path are removed (env `ECA_DIR` likewise dropped or renamed `CORPUS_DIR`)
- [ ] Reads the three INPUT_FILES (articles.jsonl, eud_book.jsonl, cafebook.jsonl) from the corpus dir; same JsonlRow/CorpusDoc parsing
- [ ] `cargo build --release --manifest-path ci/Cargo.toml --bin build_rag_index` passes; clippy/fmt clean
- [ ] A unit/smoke test (or `--help`) confirms the default corpus path resolves to ci/corpus