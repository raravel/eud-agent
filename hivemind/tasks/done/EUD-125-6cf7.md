---
completed_at: '2026-06-09T12:46:38.031487'
created: '2026-06-09'
depends_on: []
id: EUD-125-6cf7
parent: EUD-094-e9ee
priority: high
scope:
- src-tauri/src/journal.rs
- src-tauri/src/lib.rs
status: done
title: Change journal + changeset rollback (inverse ops, persistence)
type: task
updated: '2026-06-09'
---

## Description
New `src-tauri/src/journal.rs`: per-write snapshots BEFORE mutating (dat/xdat/tbl/req/btn old value +
was_default; file_write old content; file_create/mkdir created marker; file_delete full content +
position; rename/move old path; set_main old path; settings/plugins old value/Texts/index). Persist the
per-request journal to `%appdata%\eud-agent\journal\<request-id>.json` (UTF-8 no BOM). Emit
`changeset{request_id, items[]}` on turn completion (dat grouped per objId with property/old/new; file
items kind created|modified|deleted + server-side unified diff for modified). Implement
`changeset_decision`: reject -> inverse ops via the bridge in reverse seq order; accept -> archive.
Mixed per-item decisions; undecided default-accept on the next request. Runs as a background task, one
decision at a time (EUD-070).

## Spec References
- [[features/11_rust-backend-core|11_rust-backend-core]] `../docs/features/11_rust-backend-core.md` — change journal and rollback
- [[features/05_agent-core|05_agent-core]] `../docs/features/05_agent-core.md` — journal, inverse ops, background decisions
- [[rules]] `../docs/rules.md` — map-write rails (location/player inverse, no defrag)

## Completion Criteria
- [ ] snapshot + inverse-op correctness per tool kind (snapshot->rollback round-trip against a fake bridge)
- [ ] changeset grouping + unified diff for modified files; reverse-seq rollback; accept archives the journal
- [ ] journal JSON persisted UTF-8 no BOM; `cargo test` + `cargo clippy --workspace --all-targets -- -D warnings` pass