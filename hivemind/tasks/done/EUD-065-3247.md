---
completed_at: '2026-06-05T18:00:13.331602'
created: '2026-06-05'
depends_on:
- EUD-063-b479
- EUD-064-acf5
id: EUD-065-3247
parent: EUD-047-fd09
priority: high
scope:
- panel/src
- panel/components
- panel/package.json
- panel/package-lock.json
- panel/vite.config.ts
- panel/index.html
- server/tests/test_panel_static.py
status: done
title: Panel chat surface on AI Elements + Streamdown (Message/PromptInput/Plan/Reasoning/Tool,
  reset button)
type: task
updated: '2026-06-05'
---

## Goal

Rebuild the panel chat surface on vendored Vercel AI Elements + Streamdown (user decision 2026-06-05, [[decisions/06_ai-elements-streamdown-adoption]]), fixing the three reported rendering defects (reasoning invisible / answer faint / raw event kinds leaking) and adding the [새 대화] reset control.

## Mandatory components (user-specified)

`Message`, `PromptInput`, `Plan` (plan approval), `Reasoning`. Adopted alongside (orchestrator selection): `Conversation` (auto-scroll), `Response` (Streamdown message body), `Tool` (tool-call rows), `Loader`. Vendor as SOURCE under `panel/components/ai-elements/` (shadcn registry fetch at dev time, committed); `streamdown` as an npm dependency (bundled — never a runtime CDN; rules.md no-CDN scan must stay green).

## Surfaces

- **Conversation/Message**: user bubbles + agent answers as PROMINENT (foreground) Message bubbles rendered via Streamdown in real time (`delta` agent_event text streams into the live message). System/progress/info rows stay muted.
- **Reasoning**: `reasoning` agent_event deltas accumulate into the Reasoning component — dim, GPT-style, auto-open while streaming, collapses when the answer starts. Reset per turn.
- **Tool**: `tool_call`/`tool_result` events render as Tool rows by tool name (도구 호출 n건 summary retained). Raw kinds (`delta`, `answer`, `token_usage`, `turn_done`, `item_started`, `item_completed`, `event`) MUST NEVER appear as literal UI text (leak test required).
- **Plan**: ai-elements Plan component; markdown via Streamdown (replaces the EUD-060 hand-rolled renderer in PlanView). 피드백/승인 wiring unchanged (`plan_feedback`/`plan_approve`).
- **PromptInput**: replaces the InstructionBox textarea; send gating v2 unchanged (`connected && hasProject && !busy`); [새 대화] control sends `reset{}` (protocol.ts addition per features/05) and clears client log/plan/changeset; disabled while a turn is in flight.
- **ChangesetView**: restyled with ai-elements primitives; diff body UNCHANGED — server unified diff text with +/- coloring (NEVER Monaco DiffEditor); keep `lib/changeset.ts` itemKey/itemIds helpers and decision flows intact.

## Completion criteria

- [ ] Vendored ai-elements source committed under `panel/components/ai-elements/` (mandatory 4 + adopted set); `streamdown` in package.json; build green with NO external origins in dist (test_panel_static external-origin scan).
- [ ] Reasoning deltas render dim/collapsible (auto-open streaming, collapse on answer); answer deltas stream into a prominent Streamdown-rendered Message; both reset per turn (component tests).
- [ ] No-raw-kind-leak test: feeding a noise-kind event sequence surfaces none of the literal kind strings.
- [ ] Final `answer{}` log entries render prominent (foreground), not muted (test asserts the styling inversion).
- [ ] Plan card renders via the Plan component + Streamdown; feedback/approve flows pass existing suites (updated as needed).
- [ ] [새 대화] sends `reset{}` + clears client state; disabled mid-turn (test).
- [ ] ChangesetView keeps all decision/rollback behaviors (existing suites green, restyled).
- [ ] Full vitest suite + `npm --prefix panel run build` + server pytest (panel static) green.

## Spec References

- [[features/06_changeset-review-panel|06_changeset-review-panel]] — UI foundation + Behaviors (amended 2026-06-05)
- [[features/05_agent-core|05_agent-core]] — `reset{}`, `reasoning`/`delta` agent_event kinds
- [[decisions/06_ai-elements-streamdown-adoption]]

## Notes

- depends_on EUD-063 (server reasoning/delta events) + EUD-064 (`reset{}` server side).
- Registry fetch happens at DEV time (committing source); if network-restricted in the worktree, vendor from the ai-elements GitHub source instead — same outcome, document which path was used.
- Korean labels, 500-entry log cap, send gating v2 retained throughout.