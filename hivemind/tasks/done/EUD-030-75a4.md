---
completed_at: '2026-06-04T22:12:50.466228'
created: '2026-06-04'
depends_on: []
id: EUD-030-75a4
parent: EUD-026-4c16
priority: medium
scope:
- server/eud_agent/config.py
- server/eud_agent/__main__.py
- server/tests/test_config.py
status: done
title: 'Config robustness: bad agent.cfg diagnostic, port validation, HF env var'
type: chore
updated: '2026-06-04'
---

## Description
Config robustness chore (EUD-009 review advisories): (1) an agent.cfg that exists but fails to parse (BOM'd, malformed JSON, non-dict) must surface as a DISTINCT selfcheck diagnostic ("agent.cfg present but unparseable at <path>") instead of silently falling back to defaults — resolution still proceeds with defaults (the server must boot), but the operator sees why their cfg was ignored; (2) a non-numeric `port` from env/cfg must produce a clean selfcheck failure line (not an uncaught ValueError traceback) — invalid port falls back to the default with a diagnostic; (3) replace the invented `HF_HOME_HUB` env var with standard HuggingFace resolution: honor `HF_HUB_CACHE` then `HF_HOME` (hub subdir) then the current default path. Update tests for all three (BOM'd cfg fixture, garbage port, env-var precedence).

## Spec References
- [[features/02_python-server|02_python-server]] `../docs/features/02_python-server.md` — config.py
- [[verify]] `../docs/verify.md` — Stage: smoke ("agent.cfg schema" claim this chore makes true)
- EUD-009 report `../tasks/_reports/EUD-009-6aed-report.md` — Review advisories 1-3

## Completion Criteria
- [ ] BOM'd/malformed agent.cfg → selfcheck exits non-zero IF the cfg was explicitly pointed at (env/CLI), with its own message; auto-discovery of a broken cfg surfaces a warning line while resolution proceeds on defaults
- [ ] Non-numeric port (env or cfg) → no traceback; diagnostic line + default port used
- [ ] HF cache resolution honors HF_HUB_CACHE > HF_HOME > default; `HF_HOME_HUB` removed
- [ ] New/updated tests cover all three; full pytest suite passes; ruff clean; `--selfcheck` still exits 0 on this machine