---
completed_at: '2026-06-04T20:25:36.997563'
created: '2026-06-04'
depends_on:
- EUD-031-01ec
id: EUD-033-a00f
parent: EUD-026-4c16
priority: high
scope:
- panel/src/**
- panel/package.json
- panel/package-lock.json
- panel/vitest.config.ts
- panel/tsconfig.app.json
status: done
title: Panel WS protocol client + state machine (typed, reconnect, log cap)
type: task
updated: '2026-06-04'
---

## Description
Typed WS protocol client + panel state machine in `panel/src/` (no UI yet): `src/ws/client.ts` (token from location.search via URLSearchParams; `ws://${location.host}/ws?token=`; 2s-backoff auto-reconnect that re-requests `status`+`list` on open; FIX vs vanilla: track was-open/onerror state so an outage logs ONE disconnect entry, not one per retry cycle), typed message definitions for the full protocol (client: instruct/apply/status/list; server: progress/code/applied/error/status/list; unknown types tolerated and surfaced as log entries, never thrown), `src/state/` store implementing the spec state machine (connecting/retry/ready/working/reviewing/applying/waiting) with an event-log capped at 500 entries (drop oldest), empty-but-open-project Send gating (SET needs a settable target; new-file mode stays available), and an `applying`-reconnect reset to ready. Framework-agnostic core (plain TS + listener pattern or zustand-style store implemented in-repo — NO new runtime dependency without recording it for tech-stack re-grounding).

Add headless unit tests (vitest, dev-dependency) for: reconnect/backoff scheduling, message dispatch incl. unknown types, log cap, state transitions per the spec mermaid diagram, NEWEPS filename validation (non-empty after trim; no `/` or `\\`).

## Spec References
- [[features/03_agent-panel|03_agent-panel]] `../docs/features/03_agent-panel.md` — Flow / state machine, Behaviors
- [[architecture]] `../docs/architecture.md` — WebSocket protocol (panel to server)

## Completion Criteria
- [ ] `npm --prefix panel run build` exit 0 (typechecks)
- [ ] vitest suite passes (`npm --prefix panel test` script added): transitions, reconnect single-log fix, 500-entry log cap, unknown-type tolerance, NEWEPS validation
- [ ] Protocol types cover every message in architecture.md both directions
- [ ] Full pytest suite still passes (no server-side impact)