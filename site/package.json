{
  "name": "eud-agent-site",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the eud-agent GitHub Pages install page. Compiles Tailwind to ../docs/assets/app.css (the docs/ folder is what Pages serves).",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ../docs/assets/app.css --minify",
    "watch": "tailwindcss -i ./input.css -o ../docs/assets/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0"
  }
}
